package com.selenium.driver.downloader.SeleniumBrowserDriverDownloader.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RestController;

import com.selenium.driver.downloader.SeleniumBrowserDriverDownloader.service.DownloadService;

@RestController
public class DownloadController {

	/*@Autowired
	private DownloadService service;
	
	@GetMapping("/hello")
	public String getmessage() {
		return "Hello Mohit";
	}

	@GetMapping("/browser/{bName}/version/{ver}")
	public String getDriver(@PathVariable String bName, @PathVariable String ver) {
		return "Driver";
	}*/
	
	
}
